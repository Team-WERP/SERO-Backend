<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.werp.sero.client.query.dao.ClientAddressMapper">

    <!-- ResultMap 정의 -->
    <resultMap id="clientAddressResultMap" type="com.werp.sero.client.query.dto.ClientAddressResponseDTO">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="address" column="address"/>
        <result property="recipientName" column="recipient_name"/>
        <result property="recipientContact" column="recipient_contact"/>
        <result property="isDefault" column="is_default"/>
    </resultMap>

    <!-- 고객사별 배송지 목록 조회 -->
    <select id="findByClientId" resultMap="clientAddressResultMap">
        SELECT
               A.id,
               A.name,
               A.address,
               A.recipient_name,
               A.recipient_contact,
               A.is_default
          FROM client_address A
         WHERE A.client_id = #{clientId}
         ORDER BY A.is_default DESC, A.created_at DESC
    </select>       

</mapper>
    





